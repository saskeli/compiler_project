namespace mpl.domain
{
    public enum TokenType
    {
        Name,
        String,
        Number,
        Real,
        Control
    }

    public enum Type
    {
        Int,
        String,
        Bool
    }

    public enum Ctype
    {
        Num,
        Char,
        Control,
        Whitespace
    }
}
